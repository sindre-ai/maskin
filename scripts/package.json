{
	"name": "@maskin/scripts",
	"version": "0.0.1",
	"private": true,
	"license": "Apache-2.0",
	"type": "module",
	"description": "Repo tooling scripts (CI gates, one-off maintenance). Not published.",
	"scripts": {
		"test": "vitest",
		"type-check": "tsc --noEmit",
		"bet-surface-probe-check": "tsx bet-surface-probe-check.ts"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"tsx": "^4.19.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
