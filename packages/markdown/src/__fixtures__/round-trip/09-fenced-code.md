# Fenced code blocks

A TypeScript block with a language info string.

```ts
export function greet(name: string): string {
	return `hello, ${name}`
}
```

A shell block with an info string that includes meta.

```bash showLineNumbers
echo "hello"
echo "world"
```

A plain fence with no info string.

```
first line
second line
```
